## 选中 zhouhe-dark

从命令面板或下面的链接打开主题选择器，选中 **zhouhe-dark**。

[选择主题](command:workbench.action.selectTheme)

这是暗色主题（`uiTheme: vs-dark`），选中后 VS Code 的整体基调会切到暗色。

如果列表里没有它，说明主题扩展被禁用了：到扩展面板搜索 `zhouhe-dark` 重新启用即可。
